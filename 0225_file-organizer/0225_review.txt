身についたこと
辞書を使った効率化: 拡張子とフォルダ名のセットを辞書で管理し、ループで一括処理する方法。

is_file() による安全ガード: フォルダそのものを移動対象から外し、ファイルだけを処理する重要性。

実行場所の制御: Path(__file__).parent を使い、プログラムの場所を基準に動かすテクニック。

実務で役にたつ想定場面
PCのデスクトップ整理: 散らかったファイルを、拡張子ごとに「画像」「書類」「ログ」フォルダへ一瞬で仕分ける。
